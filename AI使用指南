1.如何省token——上下文篇
首先，可以直接删掉不相关的上下文。因为每次系统都会把以往的对话打包成一个json交给模型，直接删去就能生效。
其次，可以进行上下文折叠，生成一份摘要，让新窗口知道之前大致发生了什么事。但是折叠之后一定会损失具体数据细节
“由于当前上下文过长，我们需要迁移阵地。请对我们迄今为止的所有对话进行极其硬核的结构化提炼（Memory Snapshot）。
    转移前，prompt必须包含以下模块：
核心目标： 我们正在试图解决的终极问题是什么？
已确立的物理法则/规则： 我们在对话中定下了哪些不可违背的执行纪律或系统指令？
已推演出的核心结论： 用分点形式列出我们已经得出的最重要事实。
遗留问题： 我们下一步正准备探讨的痛点是什么？
请以极度精简的代码注释风格或 Markdown 输出，摒弃所有客套话。”
    转移后（在新窗口的开局注入）：
在新窗口的 System Instructions 中填好你的“架构师”角色设定。
将旧窗口生成的“结构化提炼报告”粘贴进对话框。
并在末尾加上一句触发语：“以上是我为你注入的先验知识快照。已阅请回复‘环境加载完毕’，我将提出下一个问题。”


2.长上下文窗口尽量不要大幅度改变instruction
因为新指令会被依据老指令生成的内容所稀释，所以效能有所降低
3.更换模型不会有其他后果，只要确保上下文窗口可以

pip freeze > requirements.txt
pip install -r requirements.txt